{% set name = "pypac" %}
{% set version = "0.18.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/carsonyl/pypac/archive/refs/tags/v{{ version }}.tar.gz
  sha256: e2cee9b73c4c4ef7219cdb1bb37523d5cae4da5b79a1def694b34bccd9025ca0

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - requests >=2.0.0
    - tldextract >=2.2.3
    - dukpy >=0.2.2
    - pyobjc-framework-SystemConfiguration >=3.2.1  # [osx]
    # Keep the package noarch but allow for the osx only dep
    - __linux  # [linux]
    - __osx    # [osx]
    - __win    # [win]

test:
  imports:
    - pypac
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/carsonyl/pypac
  summary: Proxy auto-config and auto-discovery for Python.
  license: Apache-2.0
  license_file:
    - LICENSE

extra:
  recipe-maintainers:
    - rxm7706
